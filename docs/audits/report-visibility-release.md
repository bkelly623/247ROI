# Existing visibility report release patch

Reviewed original dirty-tree checkpoint and owned source diffs before adopting into isolated production worktree. Original tree preserved. No production modules, Opportunity/hire, PresentView, email adapters or routes imported/edited by this task.

## Changes
- Native CTA links retain visibility session context; report analytics nonblocking.
- Exact sampled queries, provider/location/time, sampled match versus unmeasured distinctions; organic matches require audited hostname/subdomain. Places ordering not an organic rank. Unknown reviews do not become zero; unmatched listing does not establish inexistence.
- 247ROI automation context and unknown-industry fallback; name inference explicitly unconfirmed in UI.
- Retired schema-derived AI percentage and composite search percentage, forced Foundation ladder, speculative simulator and generic industry statistics from this report renderer. Existing design/cards retained. Legacy opportunityIndex number remains for contract compatibility, not rendered as a score.
- Targeted service recommendations linked to observed deficits, with suggestions separate from measurements. Missing LocalBusiness schema not treated as a universal defect. Preview annotation positions/after states explicitly illustrative, not deployed fixes.
- Existing Google requests have 25s signals; enrichment 15s signal covers body processing. Provider raw bodies/errors no longer logged by these owned adapters. LLM cannot overwrite deterministic findings, headline or packages; optional advisor output remains suggestions.
- Optional Google AI Overview answer/reference extraction uses existing organic response, never follows async tokens or requests additional collection. Missing answer is unmeasured, not brand absent. ChatGPT and Google AI Mode remain unmeasured.

## Additive contract
`GoogleAIOverviewEvidence`: query, observedAt, location, source='serpapi', state='observed'|'not_returned'|'unavailable', optional answer, citations[{title,url}]. Available as `GoogleSearchAudit.aiOverviews` and persisted/rendered as `AuditReport.googleLocal.aiOverviews`. Existing fields remain.

## Real verification
- `npx --no-install tsx scripts/audit-report-ux.test.ts` passed initial expanded suite including source CTA contract, inference, host spoof rejection, sample provenance, Overview observed/missing/placeholder parsing, no fabricated empty-sample rank/review deficit, and no forced score/simulator template.
- `git diff --check` passed at that point.
- `npm run lint` executed: no owned errors; four errors in concurrently created scripts/report-email.test.cjs (no-require-imports), plus warnings. Parent/email owner must resolve.
- Subsequently added full offline runAuditPipeline/Organization-schema and default-advisor assertions. Attempt to rerun regression + diff + targeted lint was held pending runtime approval because eslint package threat-intelligence metadata timed out. No retry/bypass attempted. These newest assertions require execution by parent after approval. Prior passed suite remains verified but is not represented as verification of later additions.
- No build, browser acceptance, provider request, paid call, commit or deployment performed by this task. Parent owns build/release and live readback.
